name: healthcare_claims_denial
version: 1.0.0
config-version: 2

profile: healthcare_claims_denial

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  healthcare_claims_denial:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
