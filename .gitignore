# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Data — synthetic CSVs are committed so the Streamlit Cloud demo works
# without a generation step (they're small and contain no PHI/PII)
data/cms/*

# ML artifacts (.pkl committed for the cloud demo risk scorer)

# dbt
dbt_project/target/
dbt_project/dbt_packages/
dbt_project/logs/
dbt_project/profiles.yml

# Secrets / credentials
.env
*.pem
snowflake_credentials*

# OS / editor
.DS_Store
.vscode/
.idea/
